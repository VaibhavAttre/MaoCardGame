using Riptide;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;

public static class NetworkEvents
{

    /*
    public static event UnityAction<Message> SendMessageToAll;
    public static void OnSendMessage(Message message) => SendMessageToAll?.Invoke(message);

    public static event UnityAction<Message, ushort> SendMessageToPlayer;
    public static void OnSendMessage(Message message, ushort id) => SendMessageToPlayer?.Invoke(message, id);
    */
}
